package com.elihimas.weatherapp.models

import com.elihimas.weather.data.model.Forecast
import com.elihimas.weather.data.model.Weather

data class MainData(val weather: Weather, val forecast: Forecast)
